export function Bar({children}) {
    return (
        <div className="w-full h-[10vh] text-2xl  overflow-hidden flex justify-center items-center text-center">
            {children}
        </div>
    );
}
